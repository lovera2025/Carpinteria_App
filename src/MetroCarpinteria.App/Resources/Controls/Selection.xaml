<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        CheckBox y RadioButton. Los originales de WPF son chicos y de color azul del
        sistema: al lado del resto de la app se ven como de otro programa, y el área
        clicable de 13 píxeles es incómoda en una notebook de taller.
    -->

    <Style TargetType="CheckBox">
        <Setter Property="FontFamily" Value="{DynamicResource AppFont}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}" />
        <Setter Property="Foreground" Value="{DynamicResource ContentBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="10,0,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <!-- El Border exterior hace clicable también la etiqueta -->
                    <Border Background="Transparent" Padding="2" CornerRadius="{DynamicResource RadiusSm}">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="box"
                                    Width="20" Height="20"
                                    Background="{DynamicResource SurfaceRaisedBrush}"
                                    BorderBrush="{DynamicResource BorderStrongBrush}"
                                    BorderThickness="{DynamicResource BorderMedium}"
                                    CornerRadius="{DynamicResource RadiusSm}"
                                    VerticalAlignment="Center">
                                <TextBlock x:Name="check"
                                           Text="✓"
                                           FontSize="13"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource ContentOnBrandBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="box" Property="Background" Value="{DynamicResource BrandPrimaryBrush}" />
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource BrandPrimaryBrush}" />
                            <Setter TargetName="check" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource BrandPrimaryHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource BorderFocusBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="FontFamily" Value="{DynamicResource AppFont}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}" />
        <Setter Property="Foreground" Value="{DynamicResource ContentBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="10,0,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border Background="Transparent" Padding="2" CornerRadius="{DynamicResource RadiusSm}">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="20" Height="20" VerticalAlignment="Center">
                                <Ellipse x:Name="ring"
                                         Fill="{DynamicResource SurfaceRaisedBrush}"
                                         Stroke="{DynamicResource BorderStrongBrush}"
                                         StrokeThickness="2" />
                                <Ellipse x:Name="dot"
                                         Width="10" Height="10"
                                         Fill="{DynamicResource BrandPrimaryBrush}"
                                         Visibility="Collapsed" />
                            </Grid>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource BrandPrimaryBrush}" />
                            <Setter TargetName="dot" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource BrandPrimaryHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource BorderFocusBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Interruptor. Se usa para las preferencias de Configuración, donde el cambio
        se aplica en el momento: un tilde sugiere que hay que guardar después.
    -->
    <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
        <Setter Property="FontFamily" Value="{DynamicResource AppFont}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}" />
        <Setter Property="Foreground" Value="{DynamicResource ContentBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Border Background="Transparent" Padding="2">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="track"
                                    Width="42" Height="24"
                                    Background="{DynamicResource SurfaceSunkenBrush}"
                                    BorderBrush="{DynamicResource BorderStrongBrush}"
                                    BorderThickness="{DynamicResource BorderThin}"
                                    CornerRadius="{DynamicResource RadiusPill}"
                                    VerticalAlignment="Center">
                                <Ellipse x:Name="knob"
                                         Width="16" Height="16"
                                         Fill="{DynamicResource ContentMutedBrush}"
                                         HorizontalAlignment="Left"
                                         Margin="4,0,0,0" />
                            </Border>
                            <ContentPresenter Margin="12,0,0,0" VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="track" Property="Background" Value="{DynamicResource BrandPrimaryBrush}" />
                            <Setter TargetName="track" Property="BorderBrush" Value="{DynamicResource BrandPrimaryBrush}" />
                            <Setter TargetName="knob" Property="Fill" Value="{DynamicResource ContentOnBrandBrush}" />
                            <Setter TargetName="knob" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="knob" Property="Margin" Value="0,0,4,0" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="track" Property="BorderBrush" Value="{DynamicResource BorderFocusBrush}" />
                            <Setter TargetName="track" Property="BorderThickness" Value="{DynamicResource BorderMedium}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
