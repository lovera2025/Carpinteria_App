<UserControl x:Class="MetroCarpinteria.App.Views.SectionPlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid x:Name="PageRoot"
          Background="{StaticResource ContentBackgroundBrush}"
          Opacity="0">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    MaxWidth="480">
            <TextBlock Text="{Binding Icon}"
                       FontSize="56"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20" />
            <TextBlock Text="{Binding Title}"
                       Style="{StaticResource PageTitleStyle}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8" />
            <TextBlock Text="{Binding Subtitle}"
                       Style="{StaticResource SubtitleStyle}"
                       TextAlignment="Center"
                       Margin="0,0,0,24" />
            <Border Background="#1A6B4423"
                    CornerRadius="12"
                    Padding="20,14"
                    HorizontalAlignment="Center">
                <TextBlock HorizontalAlignment="Center"
                           Foreground="{StaticResource PrimaryBrownBrush}"
                           FontSize="14">
                    <Run Text="Próximamente en " />
                    <Run Text="{Binding ComingSoonPart, Mode=OneWay}" FontWeight="SemiBold" />
                </TextBlock>
            </Border>
        </StackPanel>
    </Grid>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="PageRoot"
                                     Storyboard.TargetProperty="Opacity"
                                     From="0"
                                     To="1"
                                     Duration="0:0:0.35">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut" />
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>
