<Window x:Class="MetroCarpinteria.App.Views.QuotePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Vista previa"
        Height="820" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ContentBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock x:Name="PageCountText" FontSize="12" Margin="0,2,0,0"
                           Foreground="{DynamicResource TextSecondaryBrush}" />
            </StackPanel>

            <WrapPanel Grid.Column="1" VerticalAlignment="Center">
                <Button Content="Imprimir" Style="{StaticResource PrimaryButtonStyle}"
                        Click="OnPrint" Margin="0,0,10,0" />
                <Button x:Name="SavePdfButton" Content="Guardar PDF"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="OnSavePdf" Margin="0,0,10,0" />
                <Button Content="Cancelar" Style="{StaticResource SecondaryButtonStyle}"
                        Click="OnCancel" IsCancel="True" />
            </WrapPanel>
        </Grid>

        <!-- Las hojas tal como salen impresas: es el mismo dibujo que verifica el runner
             de documentos, no una maqueta parecida. -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto"
                      Background="{DynamicResource SurfaceSunkenBrush}"
                      Padding="16">
            <ItemsControl x:Name="PagesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,16" BorderBrush="{DynamicResource BorderBrushToken}"
                                BorderThickness="1" SnapsToDevicePixels="True">
                            <Image Source="{Binding}" Stretch="None" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
