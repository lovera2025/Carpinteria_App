<UserControl x:Class="MetroCarpinteria.App.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:MetroCarpinteria.App.Models"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="700"
             d:DesignWidth="1000">
    <Grid x:Name="PageRoot" Background="{StaticResource ContentBackgroundBrush}" Opacity="0">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32,24">
            <StackPanel MaxWidth="1000">
                <Grid Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Reportes" Style="{StaticResource PageTitleStyle}" Margin="0,0,0,4" />
                        <TextBlock Text="Resumen general del taller."
                                   Style="{StaticResource SubtitleStyle}" />
                    </StackPanel>
                    <Button Content="Actualizar"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding LoadCommand}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Padding="16,10" />
                </Grid>
                <TextBlock Margin="0,0,0,20" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12">
                    <Run Text="Generado: " />
                    <Run Text="{Binding GeneratedAt, Mode=OneWay}" FontWeight="SemiBold" />
                </TextBlock>

                <ItemsControl ItemsSource="{Binding Sections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ReportSection}">
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                        <TextBlock Text="{Binding Icon}" FontSize="22" Margin="0,0,12,0" />
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding Metrics}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type models:ReportMetric}">
                                                <Border Padding="0,8" BorderBrush="#123D2914" BorderThickness="0,0,0,1">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding Label}"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       FontSize="13" />
                                                            <TextBlock Text="{Binding Detail}"
                                                                       FontSize="11"
                                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                                       Opacity="0.85"
                                                                       Margin="0,2,0,0"
                                                                       Visibility="{Binding Detail, Converter={StaticResource StringToVisibilityConverter}}" />
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1"
                                                                   Text="{Binding Value}"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="15"
                                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                                   VerticalAlignment="Center" />
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="PageRoot" Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.35" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>
